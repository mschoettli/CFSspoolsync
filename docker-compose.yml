version: "3.8"

services:
  cfsspoolsync:
    image: ghcr.io/mschoettli/cfsspoolsync:latest
    container_name: cfsspoolsync
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/app/data
      - /root/.ssh/id_k2:/root/.ssh/id_k2:ro
    env_file:
      - .env
